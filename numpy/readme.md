set up : pip install numpy
import import numpy as np


- Tất cả phần tử phải cùng dữ liệu
- Kích thước không thể thay đổi
- phải là shape kiểu rectangular

* tổng quát cho array nhiều chiều
- column index sẽ là cái cuối cùng (axis cuối)'
- row index sẽ là cái cận cuối cùng ( axis cuối - 1)


mean = tổng tất cả phần tử / số lượng phần tử


** Broadcasting 

** reshape  and flatten

